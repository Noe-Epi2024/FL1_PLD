part of '../../profile_page.dart';

class _ProfilePageDesktop extends StatelessWidget {
  const _ProfilePageDesktop();

  @override
  Widget build(BuildContext context) =>
      const AppLayoutDesktop(child: _ProfilePageContent());
}
