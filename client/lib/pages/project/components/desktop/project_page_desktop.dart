part of '../../project_page.dart';

class _ProjectPageDesktop extends StatelessWidget {
  const _ProjectPageDesktop();

  @override
  Widget build(BuildContext context) => const Scaffold(
        body: AppLayoutDesktop(child: _ProjectPageContent()),
      );
}
